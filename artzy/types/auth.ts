export interface SignupCredential{
    fullName: string;
    address: string;
    email: string;
    password: string;
    phoneNumber: string;
}

export interface SigninCredential{
    fullName: string;
    address: string;
    email: string;
    password: string;
    phoneNumber: string;
}